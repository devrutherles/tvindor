import Header from "@/app/(app)/Header";
import Home from "@/components/Home";

export const metadata = {
  title: "TV Indoor - Dashboard",
};

const Dashboard = () => {
  return (
    <>
      <Home />
    </>
  );
};

export default Dashboard;
